{% extends 'layout/layout.twig' %}

{% block content %}
    <div class="page-header">
        <h1>Sign In</h1>
    </div>

    {{ form(loginForm) }}

    {% if enableRegistration %}
        Not registered? <a href="{{ url('zfcuser/register') }}?redirect={{ redirect }}">Sign up!</a>
    {% endif %}

{% endblock content %}
